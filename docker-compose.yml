services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: profitradar-app
    restart: unless-stopped
    ports:
      - "3004:3000"
    environment:
      - NODE_ENV=production
