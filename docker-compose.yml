services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: profitradar-app
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
